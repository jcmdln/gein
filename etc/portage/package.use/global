# /etc/portage/package.use/global

## Blacklist
*/* -aqua -athena -avahi -bindist -cairo -doc -games -gnome-keyring -gtk -gtk2 -handbook -introspection -kdesu -kwallet -llvm -multilib -offensive -openssl -qt4 -sslv3 -static -static-libs -systemd -tls-heartbeat -wayland

## Base
*/* deblob gnutls hardened minimal nplt threads unicode

## Minimal
*/* dbus bzip2 zlib

## Desktop
# */* alsa bluetooth pulseaudio
# */* ffmpeg gif imagemagick jpeg jpeg2k png svg tiff xpm
# */* X egl gles openal opengl sdl sdl2 vaapi vdpau xft
